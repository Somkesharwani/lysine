spring:
  application:
    name: reimbursement-service
  datasource:
    url: jdbc:postgresql://localhost:5432/lysine_db
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true