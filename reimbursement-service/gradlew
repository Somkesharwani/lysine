#!/bin/sh

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
ROOT_DIR=$(CDPATH= cd -- "$SCRIPT_DIR/.." && pwd)

if [ "$#" -gt 0 ]; then
    TASK=$1
    shift
else
    TASK=build
fi

exec "$ROOT_DIR/gradlew" -p "$ROOT_DIR" ":reimbursement-service:$TASK" "$@"
